import domElements from './indexDomElements.js';

/** STRINGS **/
export const searchBarPlaceholder = 'Search..';

/** GENERAL CONSTANTS **/
export const createPostContainerSize = domElements.userPostContainer.clientWidth;
export const userPostContainerSize = domElements.userPostContainer.clientWidth;

/** CLASSES CONSTANTS **/
export const visibleClass = 'visible';
export const selectedClass = 'selected';

export default {
  searchBarPlaceholder
};
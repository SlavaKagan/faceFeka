import React from 'react';
import { ShortInput } from '../../constants';

const SubmitInputButton = ( props ) => {
  return(
    <>
      <input type = "submit" className = "ghost"/>
    </>
  );
};

export default SubmitInputButton;